# 标签打印问题修复报告

**日期**：2026-09-15
**模块**：敏群商贸 ERP — 自定义标签打印（Web + 本地打印代理）
**涉及代码**：`api/src/routes/labels.ts`、`print-agent/lib/zpl.js`、`src/pages/LabelPrint.tsx`、需求文档 `.trae/documents/敏群商贸ERP_PRD.md`

---

## 一、问题清单（现场照片反馈）

| # | 现象 | 严重度 |
|---|------|--------|
| 1 | 一次打印 2 份标签，打印机自动多打到 9 份 | 高 |
| 2 | 字体方向不对，整个标签版面旋转 90° | 高 |
| 3 | 标签「Remark」一栏显示了 USD 价格（如 `Remark: USD4.2`），对外标签泄露内部报价 | 高（商业信息泄露） |

## 二、根因分析

| 问题 | 根因 | 依据 |
|------|------|------|
| 多打 9 份 | 份数只在数据层复制 N 个 `^XA…^XZ` 打印作业，最终份数叠加了打印机驱动的“默认份数/纸张尺寸不匹配自动补偿”，实际份数 = 系统 N × 驱动 M（2×≈4~5 或 9） | 旧代码每份标签生成独立作业，未下发份数指令 |
| 方向旋转 90° | ZPL 全部文本用 `^A0N`（正向），未显式下发方向指令，版面方向完全依赖打印机/驱动默认值 | Argox 驱动方向设置非 0° 时即偏转 |
| Remark 显示价格 | 后端 `toLabel()` 直接把物料「产品描述/产品备注」透传到标签 Remark，原始字段里含 `USD4.2` 报价 | 老系统标签模板用的是标签专用字段 |

**对照老系统（HSTIP_SHMQ）**：老系统通过汉思打印中间件 + LabelE/XLabel 模板生成 ZPL/TSPL，份数、方向、纸张尺寸均由指令/模板显式固定，不依赖驱动默认值；纸张规格固定为 70mm×40mm 铜版纸（需求规格说明书 v1.1 已写死）。新系统尺寸已对齐（`^PW559 ^LL320` = 70×40 @203dpi），唯独份数与方向未用指令固定。

## 三、修复内容

### 1. 份数改为指令级控制（zpl.js + labels.ts）
- 每张标签 ZPL 末尾追加 **`^PQ{copies},0,0,N`**，份数由打印机指令保证，与驱动设置彻底解耦。
- 后端 `/labels/preview`、`/labels/sample-choose/:id` 不再在数据层复制 N 份标签，改为每个标签返回带 `copies` 字段。
- 前端预览与浏览器打印仍按 `copies` 展开为 N 张 70×40mm 卡片（`@page size: 70mm 40mm` 不变）；走本地代理时仅发送 N 个标签（每张带 copies）。

### 2. 方向显式固定（zpl.js）
- ZPL 头部追加 **`^FWN`**（字段方向 0°），配合统一的 `^A0N` 字体指令，方向不再吃打印机默认旋转。
- 保留 `^PW/^LL` 显式标签尺寸下发（70×40mm @203dpi）。

### 3. Remark 过滤价格（前后端同规则）
- 剔除价格类片段：`USD / US$ / CNY / RMB / JPY / EUR / GBP / TWD / HKD / NT$ / $ / ￥ / ¥` + 数字，及 `#数字`。
- 过滤后为空显示 `-`；清理残留分隔符。
- 前端预览（`displayRemark()`）与代理 ZPL 渲染（`stripPrice()`）使用同一套规则，所见即所得。

### 4. 实测 ZPL 输出（输入 2 份、Remark 含 USD4.2）
```
^XA
^FWN^PW559^LL320^LH0,0          ← 方向显式 0° + 标签尺寸 70×40mm
...
^FO16,192^A0N,20,20^FDRemark: -^FS   ← USD4.2 已过滤
^PQ2,0,0,N                      ← 份数由指令控制，2 份就是 2 份
^XZ
```

### 5. 验证
- `npx tsc --noEmit -p api`：通过（0 错误）
- `npm run build`（vite + tsc）：通过（1669 模块，0 错误）
- `node -c print-agent/lib/zpl.js` + ZPL 生成实测：通过

## 四、需求文档同步更新

[敏群商贸ERP_PRD.md](file:///c:/Users/zs/Desktop/111/.trae/documents/敏群商贸ERP_PRD.md) 三处：

1. **2.1 第 11 条**：打印须与老系统对齐——份数由指令（`^PQ`/`SET COUNT`）控制、方向由指令固定，Remark 过滤 USD/CNY 报价。
2. **功能表「标签打印」行**：实际打印份数 = 系统输入份数；Remark 不显示价格。
3. **验收标准第 7 条**：输入 N 份只打 N 份、方向不旋转、Remark 无价格、Argox 实机测试（纸张 70×40mm、Gap 校准、方向 0°）。

## 五、部署与现场操作要求

| 步骤 | 内容 |
|------|------|
| 1 | 前端 + API 重新部署（已构建） |
| 2 | 打印代理机重启：`node print-agent/server.js`（zpl.js 改动在代理本地） |
| 3 | 打印机驱动：纸张设 70×40mm、方向 0°、默认份数清零 |
| 4 | 打印机做一次标签校准（Gap 检测），消除纸位偏移 |
| 5 | 实机复打验证：输入 2 份 → 打出 2 张、方向正常、Remark 无价格 |

## 六、遗留建议

- [ ] 可在代理仪表盘加「打印校准」按钮（发送 `~JC`），一键完成校准
- [ ] ZPL 换行目前按字符宽估算（`lineChars`），中英文混排可进一步改用 `^FB` 格式化域精确折行
