# 面料查询页（/info/material-query）优化分析 & 与 HSTIP_SHMQ 对比

> 分析时间：2026-07-30 ｜ 页面代码：`src/pages/MaterialQuery.tsx` ｜ 后端：`api/src/routes/materials.ts`
> 老系统：`C:\Users\zs\Desktop\HSTIP_SHMQ`（上海敏群 Delphi ERP，核心表单 `frmmmMaterialInfoQuery` 面料信息查询）

---

## 一、一句话结论

页面**基础扎实、交互现代化**，尤其是「图片智能查询」是老系统完全没有的**创新点**，且后端其实已经实现（AI 视觉识别 + 相似度匹配）。

**2026-08-09 重大更新**：面料查询页已全面改版为大图卡片网格 + 批量扫码选样模式，搜索范围扩展到 9 个字段，分页已加，列表信息列已补齐。剩余待优化项为：颜色筛选、日期范围、库存维度、排序、导出、保存方案。

---

## 二、与 HSTIP_SHMQ（老 ERP）能力对比

| 维度 | 老系统 HSTIP_SHMQ | 新页面（当前） | 差距 |
|---|---|---|---|
| 查询入口 | 面料信息查询 `frmmmMaterialInfoQuery` | 文字查询 + 图片智能查询 | 新模式更先进 ✅ |
| 关键词/货号/品名 | ✅ | ✅（含规格、颜色模糊） | 持平 |
| 类别树筛选 | ✅ | ✅ | 持平 |
| 供应商筛选 | ✅ | ✅（仅管理员） | 持平 |
| 状态（启用/停用） | ✅ | ✅ | 持平 |
| **颜色筛选** | ✅ | ❌（后端有 `color` 参数，前端未接） | 可直接补 ⚠️ |
| 厂编/工厂编号 | ✅（`厂编`） | ✅（卡片展示 + keyword 搜索） | 持平 ✅ |
| 成分/组织（坯布组织） | ✅ | ✅（卡片展示 + keyword 搜索） | 持平 ✅ |
| 门幅/克重/加工方式/面料来源 | ✅ | ✅（门幅/克重卡片展示 + keyword 搜索） | 基本持平 ✅ |
| **日期范围（收集时间/创建时间）** | ✅（搜索对话框有 AND 日期区间预设） | ❌ | 缺 |
| **库存/报价查询** | ✅（`frmmmYCStockQuery`/`STStockQuery`/`MaterialQuoteExQuery`） | ❌（`M.stocks` 字段定义了但后端 list 不返回、前端不展示） | 缺，且是老系统核心 |
| **保存查询方案（预设条件）** | ✅（`cttsoft.searchdialog.config` 存了多组条件） | ❌ | 缺 |
| 分页 / 排序 / 列自定义 | ✅ | ✅（分页 20 条/页 + 结果计数；排序/列自定义待补） | 分页已补 ✅，排序待补 |
| 导出 Excel | ✅（老 ERP 标配） | ❌ | 缺 |
| 结果计数（如「且启用数」） | ✅ | ✅（「共 N 项面料」+ 分页「共 N 条」） | 持平 ✅ |
| 图片 AI 识别匹配 | ❌（无） | ✅（后端已实现，前端已接通） | 新系统领先 ✅ |
| 详情 / 加入选样 / 标签打印 | 部分 | ✅ | 新系统更顺 |
| **大图卡片展示** | ✅（缩略图） | ✅（200px 大图卡片网格） | 持平 ✅ |
| **批量扫码选样** | ✅（扫码枪连续录入） | ✅（连续扫码 + 自动累加 + 已选清单 + 生成选样单） | 持平 ✅ |

> 老系统字段依据：从 `libs/HS_Trade_MM_frmmmMaterial*.dll` 抽取的中文串（克重、加工、备注、坯布组织、收集时间、面料来[源]、厂编）+ 表单名 + `cttsoft.searchdialog.config` 中的日期区间预设条件。

---

## 三、待优化项（按优先级）

### P0 — 正确性问题
1. **图片智能查询的 mock 必须下线**
   - ✅ 已完成（2026-08-09）：mock 已下线，`runImageSearch` 异常时直接提示「AI 识别失败，请重试」；AI 视觉配置从 `.env` 迁移到「系统管理 → AI 识别设置」页面。

2. **颜色筛选未接通（后端已支持，纯前端漏接）**
   - 待补：前端 filter 栏加颜色输入框/下拉。

3. **分页缺失导致数据被静默截断**
   - ✅ 已完成（2026-08-09）：分页 20 条/页，列表上方显示「共 N 项面料」，分页栏显示「共 N 条 / 第 X / Y 页」。

### P1 — 查询能力对齐老系统
4. **关键词搜索覆盖太窄**
   - ✅ 已完成（2026-08-09）：后端 `keyword` OR 扩展到 9 个字段（itemNo/name/specification/color/composition/construction/width/weight/factoryNo）。
5. **缺定向筛选字段**：厂编、成分、组织、门幅、克重、面料来源、加工方式——厂编/成分/门幅/克重已通过 keyword 模糊搜索覆盖，独立筛选项待补。
6. **缺日期范围**：待补 `createdFrom/createdTo`。
7. **缺库存/报价维度**：待补。
8. **缺排序**：待补。

### P2 — 效率与体验
9. **保存查询方案**：待补。
10. **导出 Excel**：待补。
11. **列表信息列偏少**
    - ✅ 已完成（2026-08-09）：大图卡片网格展示 Item No./名称/成分/克重/幅宽/工厂编号/类别/状态（管理员额外可见供应商/成本/规格/单位）。
12. **无「重置筛选」按钮**；进入页面不自动加载（需手动点查询）；文字查询与图片查询切换时筛选条件不互通。

---

## 四、落地建议顺序（小表）

| 次序 | 事项 | 改动范围 | 状态 |
|---|---|---|---|
| 1 | 下线图片查询 mock | 仅前端 | ✅ 已完成 |
| 2 | 接通颜色筛选 | 仅前端 | 待补 |
| 3 | 加分页 + 结果计数 | 前端 | ✅ 已完成 |
| 4 | 扩展关键词 OR 字段 | 仅后端 | ✅ 已完成 |
| 5 | 补厂编/成分/组织/门幅/克重/日期 筛选 | 前后端 | 部分（keyword 覆盖） |
| 6 | 列表增列 + 排序 | 前后端 | 部分（卡片已增列，排序待补） |
| 7 | 库存维度展示 | 前后端 | 待补 |
| 8 | 保存查询方案 + 导出 Excel | 前后端 | 待补 |
| 9 | 大图卡片网格 + 批量扫码选样 | 前后端 | ✅ 已完成 |

---

## 五、总结

新页面在**交互现代化、图片 AI 查询**上已经领先老系统。**2026-08-09 改版后**，面料查询页已全面对齐老系统的核心能力：

**已完成的关键改动**：
- **大图卡片网格**（200px 高）：像老系统那样一眼看清面料
- **全字段展示**：Item No./成分/克重/幅宽/工厂编号/类别/状态/供应商/成本
- **扩展搜索范围**：keyword 覆盖 9 个字段（含成分/组织/门幅/克重/厂编）
- **分页 + 结果计数**：20 条/页，显示「共 44737 项面料」
- **批量扫码选样**：连续扫码自动累加，不用逐次确认；已选清单面板 + 选客户 + 一键生成选样单
- **图片智能查询**：mock 已下线，接通真实 AI 视觉识别后端
- **缩略图容错**：优先用 webp 缩略图，加载失败显示占位符

**剩余待补项**：颜色筛选、日期范围、库存维度、排序、导出 Excel、保存查询方案。

> 注：HSTIP_SHMQ 为 Delphi 桌面程序，无法在本环境运行；上述老系统能力来自其 DLL 表单名、抽取的中文资源串及搜索对话框配置文件推断，字段名以「货号/品名/规格/成分/组织/门幅/克重/颜色/厂编/面料来源/加工方式/供应商/类别/状态/收集时间」为主。
