# 面料查询页（/info/material-query）优化分析 & 与 HSTIP_SHMQ 对比

> 分析时间：2026-07-30 ｜ 页面代码：`src/pages/MaterialQuery.tsx` ｜ 后端：`api/src/routes/materials.ts`
> 老系统：`C:\Users\zs\Desktop\HSTIP_SHMQ`（上海敏群 Delphi ERP，核心表单 `frmmmMaterialInfoQuery` 面料信息查询）

---

## 一、一句话结论

页面**基础扎实、交互现代化**，尤其是「图片智能查询」是老系统完全没有的**创新点**，且后端其实已经实现（AI 视觉识别 + 相似度匹配）。但仍有 3 类问题：

1. **正确性缺陷（必须修）**：图片查询仍跑 mock，任何后端异常都回退成假数据，生产环境会**误导用户**。
2. **筛选维度落后于老系统**：颜色筛选后端已支持却没接 UI；厂编/成分/组织/门幅/克重/日期/库存等老系统标配的筛选缺失。
3. **效率体验缺失**：无分页、无排序、无导出、无保存查询方案、列表信息列偏少。

---

## 二、与 HSTIP_SHMQ（老 ERP）能力对比

| 维度 | 老系统 HSTIP_SHMQ | 新页面（当前） | 差距 |
|---|---|---|---|
| 查询入口 | 面料信息查询 `frmmmMaterialInfoQuery` | 文字查询 + 图片智能查询 | 新模式更先进 ✅ |
| 关键词/货号/品名 | ✅ | ✅（含规格、颜色模糊） | 持平 |
| 类别树筛选 | ✅ | ✅ | 持平 |
| 供应商筛选 | ✅ | ✅（仅管理员） | 持平 |
| 状态（启用/停用） | ✅ | ✅ | 持平 |
| **颜色筛选** | ✅ | ❌（后端有 `color` 参数，前端未接） | 可直接补 ⚠️ |
| 厂编/工厂编号 | ✅（`厂编`） | ❌ | 缺 |
| 成分/组织（坯布组织） | ✅ | ❌ | 缺（DLL 中见「坯布组织」） |
| 门幅/克重/加工方式/面料来源 | ✅ | ❌（仅进 keyword 模糊） | 缺 |
| **日期范围（收集时间/创建时间）** | ✅（搜索对话框有 AND 日期区间预设） | ❌ | 缺 |
| **库存/报价查询** | ✅（`frmmmYCStockQuery`/`STStockQuery`/`MaterialQuoteExQuery`） | ❌（`M.stocks` 字段定义了但后端 list 不返回、前端不展示） | 缺，且是老系统核心 |
| **保存查询方案（预设条件）** | ✅（`cttsoft.searchdialog.config` 存了多组条件） | ❌ | 缺 |
| 分页 / 排序 / 列自定义 | ✅ | ❌（DataTable 无分页无排序，硬编码 `pageSize=100`） | 缺 |
| 导出 Excel | ✅（老 ERP 标配） | ❌ | 缺 |
| 结果计数（如「且启用数」） | ✅ | ❌（列表无总条数提示） | 缺 |
| 图片 AI 识别匹配 | ❌（无） | ✅（后端已实现，前端待接通） | 新系统领先 ✅ |
| 详情 / 加入选样 / 标签打印 | 部分 | ✅ | 新系统更顺 |

> 老系统字段依据：从 `libs/HS_Trade_MM_frmmmMaterial*.dll` 抽取的中文串（克重、加工、备注、坯布组织、收集时间、面料来[源]、厂编）+ 表单名 + `cttsoft.searchdialog.config` 中的日期区间预设条件。

---

## 三、待优化项（按优先级）

### P0 — 正确性问题（建议立即修）
1. **图片智能查询的 mock 必须下线**
   - 现状：`MaterialQuery.tsx` 的 `runImageSearch` 在 `catch` 中调用 `getMockResult()` 返回假数据，注释写着「后端未实现时用 mock 演示（TODO: 接口上线后移除整个 catch 块）」。
   - 事实：后端 `POST /materials/image-search` 已上线（`recognizeFeatures`+`matchMaterials`，`AI_VISION_*` 已在 `.env` 配置）。
   - 风险：AI 服务抖动/超时 → 用户看到**假匹配结果**，严重误导业务决策。
   - 做法：删除 mock 分支，异常时明确提示「AI 识别失败，请重试」；保留上传/识别的加载态即可。

2. **颜色筛选未接通（后端已支持，纯前端漏接）**
   - 后端 `listSchema` 已含 `color`，`M` 类型也有 `color` 列。前端 filter 栏加一个颜色输入框/下拉即可，零后端改动。

3. **分页缺失导致数据被静默截断**
   - 现状：前端 `search()` 写死 `pageSize=100`，`DataTable` 不支持分页；若面料 >100 条，后面的**看不到且无提示**。
   - 做法：前端加分页/「加载更多」，利用后端已有的 `page`/`pageSize`（默认 20、上限 100）；建议在列表上方显示「共 N 项」。

### P1 — 查询能力对齐老系统
4. **关键词搜索覆盖太窄**：后端 `keyword` 只匹配 `itemNo/name/specification/color`；老系统能搜成分/组织/门幅/克重/厂编。建议后端 `listSchema` 的 OR 扩到 `composition/construction/width/weight/factoryNo`。
5. **缺定向筛选字段**：厂编、成分、组织、门幅、克重、面料来源、加工方式——后端 `listSchema` 需补这些可选参数，前端加对应筛选项（可折叠「高级筛选」）。
6. **缺日期范围**：后端无 `createdAt` 区间过滤；补 `createdFrom/createdTo` 即可对标老系统「收集时间」区间。
7. **缺库存/报价维度**：老系统库存查询是刚需。建议后端 list 携带 `stockQty`（聚合 `stocks.quantity`），前端管理员可看库存并支持「仅看有库存」。
8. **缺排序**：`DataTable` 无排序，后端硬编码 `updatedAt desc`；建议支持按 货号/成本/更新时间 排序。

### P2 — 效率与体验
9. **保存查询方案**：对标老系统搜索对话框预设条件，常用筛选存为方案一键切换。
10. **导出 Excel**：业务人员常用，老 ERP 标配，建议后端 `/materials/export` 或前端导出当前结果。
11. **列表信息列偏少**：当前仅 图片/货号/名称/类别/状态/(供应商·成本)/操作。建议增列：成分、门幅、克重、颜色——对选样、比料非常有用。
12. **无「重置筛选」按钮**；进入页面不自动加载（需手动点查询）；文字查询与图片查询切换时筛选条件不互通。

---

## 四、落地建议顺序（小表）

| 次序 | 事项 | 改动范围 | 工作量 |
|---|---|---|---|
| 1 | 下线图片查询 mock | 仅前端 | 小 |
| 2 | 接通颜色筛选 | 仅前端 | 小 |
| 3 | 加分页 + 结果计数 | 前端 | 中 |
| 4 | 扩展关键词 OR 字段 | 仅后端 | 小 |
| 5 | 补厂编/成分/组织/门幅/克重/日期 筛选 | 前后端 | 中 |
| 6 | 列表增列 + 排序 | 前后端 | 中 |
| 7 | 库存维度展示 | 前后端 | 中 |
| 8 | 保存查询方案 + 导出 Excel | 前后端 | 大 |

---

## 五、总结

新页面在**交互现代化、图片 AI 查询**上已经领先老系统，方向是对的。当前最该做的是 **(1) 把图片查询的假数据下线接通真实后端** 和 **(2) 补齐老系统用户已习惯的筛选维度（颜色可立即加、厂编/成分/组织/门幅/克重/日期/库存需前后端配合）**，再加 **分页/排序/导出/保存方案** 来追上老系统在「批量查、反复查」效率上的体验。

> 注：HSTIP_SHMQ 为 Delphi 桌面程序，无法在本环境运行；上述老系统能力来自其 DLL 表单名、抽取的中文资源串及搜索对话框配置文件推断，字段名以「货号/品名/规格/成分/组织/门幅/克重/颜色/厂编/面料来源/加工方式/供应商/类别/状态/收集时间」为主。
