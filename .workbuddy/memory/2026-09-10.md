# 2026-09-10 工作日志

## 项目从 111 迁移到 1112，首次启动

### 依赖安装与启动
1. 前端 `npm install`（302 packages）+ 后端 `npm install`（312 packages）
2. `prisma generate` 生成 Prisma Client（后端启动报 `AccountStatus` 导出缺失，生成后解决）
3. 重建 `api/.env`（文件缺失，按远程数据库连接和配置恢复）
4. 前端：`NODE_OPTIONS= node node_modules/vite/bin/vite.js --port 5177 --strictPort`（受管 node 22）
5. 后端：`NODE_OPTIONS= "C:/Program Files/nodejs/node.exe" node_modules/tsx/dist/cli.mjs watch src/server.ts`（node 24）

### AI 接口地址端口校验修复
- 文件：`api/src/services/ai-config.service.ts` 第 114 行
- 问题：`resolveAiBaseUrl` 中端口校验只允许 80/443，拒绝 `http://192.6.121.16:3001/v1`
- 修复：放宽为 1024–65535 范围，保留 SSRF 防护（内网/私网/localhost 仍拒绝）
- 前端 `src/pages/AiConfigSettings.tsx` 接口地址输入框提示更新，示例带端口
- 实测：用 `http://192.6.121.16:3001/v1` 测试连接，请求成功到达 AI 服务（返回 401 认证错误，端口不再被拦截）
