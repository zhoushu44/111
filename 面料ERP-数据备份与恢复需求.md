# 面料 ERP 数据备份与恢复需求

## 一、背景与目标

面料 ERP 是单 Docker 容器部署，业务数据分两处存放：

| 数据类别 | 当前存储位置 | 内容 |
| --- | --- | --- |
| 结构化业务数据 | PostgreSQL（15 张表） | 用户与角色、会话、面料资料、分类、图片记录、颜色、供应商、客户、选样单及明细、变更记录、操作日志、字典、公司信息 |
| 图片文件 | Docker Volume `fabric-erp-uploads` | 面料原图、200×200 缩略图、颜色图片、公司 Logo |

现状问题：

- 数据库无定期备份，误删、迁移失败、磁盘损坏均不可恢复。
- 图片在磁盘 Volume，与数据库分离，只备数据库会出现「记录在、图片丢」。
- **图片删除是物理删除且不可逆**：`materials.ts` 中 4 处 `unlink` 直接删除磁盘文件（原图 + 缩略图），无软删标记、无回收站。
- 数据库中存在**明文敏感数据**：AI 视觉识别密钥以明文存于 `data_dictionary` 表，`maskKey()` 仅对前端返回脱敏，数据库中未加密。
- 现有 Excel 导出（`/api/exports/materials`、`/api/exports/sample-chooses/:id`）是**业务导出**，字段不全、图片仅嵌首图、员工导出剥离成本与供应商，**不能作为灾备手段**。
- `seed.ts` 的 `upsert` 在 update 分支重置密码，生产环境执行 seed 会将管理员密码还原为硬编码值。

本需求目标：建立**可执行、可验证、可迁移**的备份与恢复方案。图片改由 COS 对象存储承载后，备份链路收敛为「数据库备份 + COS 自身配置」两条主线。

## 二、范围

### 2.1 本次包含

- 图片存储由本地 Volume 迁移至 COS 对象存储的方案与要求
- PostgreSQL 数据库的加密备份与恢复
- COS 版本控制与生命周期配置要求
- 关键配置文件（`api.env`）与密钥的备份要求
- 备份保留策略、恢复流程、验收标准

### 2.2 本次不包含

- 应用代码与镜像版本管理（由 GitHub + Docker Hub 承担）
- 高可用、主从复制、异地多活
- 备份内容的传输链路加密建设（仅要求存放与静态加密）

## 三、术语

| 术语 | 说明 |
| --- | --- |
| 全量备份 | 完整导出数据库所有表结构与数据 |
| 不可变同步 | 只上传新增文件，已存在文件跳过且不删除远端（`rclone copy --immutable`） |
| RPO | 可容忍的数据丢失时间窗口 |
| RTO | 从故障发生到服务恢复的可接受耗时上限 |
| 恢复演练 | 在非生产环境实际执行恢复并验证结果 |

## 四、功能需求

### FR-1 图片存储迁移至 COS

- FR-1.1 图片文件（原图、缩略图、颜色图片、公司 Logo）统一存储于 COS 对象存储，不再依赖本地 Volume 作为唯一副本。
- FR-1.2 数据库中图片字段须存储**对象相对 key**（如 `materials/xxx.webp`），访问域名作为配置项，不得将完整 URL 硬编码入库，以便未来更换 bucket 或接入 CDN 时无需改数据。
- FR-1.3 上传逻辑由「写本地文件」改为「上传 COS」，缩略图可继续由 sharp 生成后一并上传，或改用 COS 图片处理能力实时生成。
- FR-1.4 删除逻辑由 `unlink` 改为 COS 对象删除，且删除行为受 FR-2 版本控制保护。
- FR-1.5 存量图片须提供一次性迁移方案，迁移后须校验数据库记录与实际对象一一对应。

### FR-2 COS 版本控制与防误删

- FR-2.1 COS Bucket 必须开启**版本控制（Versioning）**，确保对象被覆盖或删除后可回滚。
- FR-2.2 建议配置生命周期规则：当前版本 30 天后转低频访问（或归档），非当前版本 90 天后过期，未完成分片上传 7 天后清理。
- FR-2.3 建议对异常批量删除配置告警。
- FR-2.4 从 COS 删除对象后，恢复单个文件应可在分钟级完成，无需翻找离线备份。

### FR-3 数据库备份

- FR-3.1 支持对 PostgreSQL 执行逻辑备份，覆盖全部 15 张业务表，包括 `_prisma_migrations` 迁移记录表。
- FR-3.2 备份须在应用不停机的前提下执行，不阻塞正常读写。
- FR-3.3 备份文件命名须含日期，格式 `fabric-erp-db-YYYYMMDD.sql.gz.gpg`。
- FR-3.4 **备份文件必须加密**：因数据库含明文 AI 密钥，未经加密的备份不得上传至对象存储或存放于共享目录。加密算法不低于 AES-256，口令与加密文件分离存放。
- FR-3.5 备份失败时须有明确错误输出，须先写临时文件、校验通过后再改名，不得产出 0 字节或截断文件。
- FR-3.6 备份须记录并导出数据校验信息（关键表行数），供恢复后比对。

### FR-4 图片引用清单

- FR-4.1 每次备份须导出图片引用清单（manifest），来源为 `material_image.url`、`material_image.thumbnail_url`、`material_color.image`、`company_info.logo_url`。
- FR-4.2 清单按日期命名并随备份一同归档，用于判断对象是否为「已被业务删除」以及追溯删除时间点。
- FR-4.3 清单本身不含敏感信息，无需加密。

### FR-5 配置与密钥备份

- FR-5.1 `api.env` 必须纳入备份范围，包含 `DATABASE_URL`、`JWT_ACCESS_SECRET`、`JWT_REFRESH_SECRET` 等。
- FR-5.2 `api.env`、GPG 加密口令、COS 访问密钥、数据库密码均须存放于密码管理器，至少保留一份离线副本。
- FR-5.3 上述密钥**不得**提交至 Git 仓库，**不得**与业务数据备份存放于同一目录。
- FR-5.4 须清理 `seed.ts` 中硬编码的明文密码，改由环境变量注入；`upsert` 的 update 分支不得重置已存在账号的密码。

### FR-6 备份执行顺序

- FR-6.1 因图片已托管 COS，备份链路简化为：导出图片清单 → 备份数据库（加密）→ 归档上传。
- FR-6.2 建议在业务低峰期执行。
- FR-6.3 备份任务须记录批次信息，便于成套恢复与追溯。

### FR-7 保留策略

- FR-7.1 数据库备份：本地保留最近 7 天，对象存储保留最近 30 天。
- FR-7.2 图片清单：保留 90 天。
- FR-7.3 图片对象：依托 COS 版本控制长期保留，由生命周期规则控制成本。
- FR-7.4 超过保留期的备份自动清理，清理前须确认新备份已成功产出。
- FR-7.5 重大操作（版本上线、批量导入、批量删除）前须手动触发一次备份，该备份不受自动清理影响。

### FR-8 恢复能力

- FR-8.1 支持将加密备份解密后恢复到全新空库，恢复后应用可正常登录并查询到原有数据。
- FR-8.2 支持按需恢复单个被删除的图片对象（通过 COS 版本回滚）。
- FR-8.3 恢复操作须有文档化步骤，具备服务器登录权限的运维人员可照做完成。
- FR-8.4 恢复后须执行数据校验：关键表行数与备份时记录一致，图片可正常显示。
- FR-8.5 恢复须在测试环境先行验证，不得直接对生产库执行。

### FR-9 迁移场景

- FR-9.1 支持将整套数据（数据库 + 配置 + COS 对象）迁移到新服务器，迁移后功能与原环境一致。
- FR-9.2 迁移后须验证：登录、面料列表与图片显示、选样单导出、标签打印均正常。

### FR-10 备份管理页（UI）

- FR-10.1 在「系统管理」菜单下新增「备份管理」入口，仅 `admin` 角色可见，与「AI 识别设置」「操作日志」同级。
- FR-10.2 备份列表须展示：备份时间、文件类型（数据库/图片清单）、文件大小、加密状态、存储位置（本地/对象存储）、执行结果（成功/失败）、失败原因。
- FR-10.3 支持按时间范围与执行结果筛选，列表分页。
- FR-10.4 支持手动触发一次备份，触发后须有明确的进行中状态，不得重复触发。
- FR-10.5 支持下载备份文件；下载加密备份时须提示该文件含敏感数据。
- FR-10.6 支持删除指定备份记录与文件；删除须二次确认，且不得删除最近一次成功备份。
- FR-10.7 备份失败时须在列表中显著标识，并可查看失败详情。
- FR-10.8 展示下次自动备份时间与保留策略摘要。

### FR-11 恢复 / 回滚页（UI）

- FR-11.1 在「备份管理」页内提供恢复入口，不单独占用菜单项。
- FR-11.2 恢复操作须展示所选备份的时间点与关键表行数，便于操作者确认目标版本。
- FR-11.3 支持恢复单个被删除的图片：输入或选择图片 key，从 COS 版本历史中列出可用版本，选择后回滚。
- FR-11.4 数据库恢复属高危操作，须**二次确认**：要求操作者手工输入指定确认文本方可执行，禁止一键无感执行。
- FR-11.5 恢复执行中须展示进度与阶段（解密 → 导入 → 校验），中途失败须明确提示当前所处阶段。
- FR-11.6 恢复完成后须展示校验结果：关键表行数比对、图片可访问性检查。
- FR-11.7 恢复全过程须写入操作日志，记录操作人、时间、备份来源、结果。
- FR-11.8 前端须提示「生产库恢复前建议先在测试环境验证」，不强制阻断。

## 五、非功能需求

| 编号 | 要求 |
| --- | --- |
| NFR-1 | 数据库备份执行时长不超过 5 分钟 |
| NFR-2 | 备份过程对线上接口响应时间影响不超过 10% |
| NFR-3 | 加密后备份文件体积不超过原始数据量的 40% |
| NFR-4 | RPO ≤ 24 小时（按日备份），RTO ≤ 1 小时 |
| NFR-5 | 图片经 CDN 回源访问，页面图片加载性能不低于迁移前 |
| NFR-6 | 备份任务须有日志记录：开始时间、结束时间、文件大小、成功/失败状态 |
| NFR-7 | COS 存储成本月均控制在 ¥20 以内（含版本控制与生命周期分层） |

## 六、验收标准

| 序号 | 验收项 | 通过标准 |
| --- | --- | --- |
| 1 | 图片迁移完成 | 存量图片全部上传 COS，数据库记录与 COS 对象一一对应，页面图片正常显示 |
| 2 | 版本控制生效 | 删除一张图片后，可通过 COS 版本回滚在分钟级恢复 |
| 3 | 数据库备份可执行 | 产出非空 `.sql.gz.gpg` 文件，解密解压后可读到建表与 INSERT 语句 |
| 4 | 备份已加密 | 直接打开备份文件为密文，无法读到明文数据与 AI 密钥 |
| 5 | 全量恢复可用 | 测试环境恢复后应用可登录，关键表行数与备份记录一致，图片正常显示 |
| 6 | 迁移可用 | 按文档迁移到新服务器，登录、查询、导出、打印均正常 |
| 7 | 保留策略生效 | 构造超期备份文件，执行清理后旧文件删除、新文件保留 |
| 8 | 密钥安全 | 确认 `api.env` 与明文密码未出现在 Git 历史；`seed.ts` 已移除硬编码密码且不再重置已有账号密码 |
| 9 | 演练通过 | 完成至少一次完整恢复演练并留存记录 |
| 10 | 备份管理页可用 | 列表正确展示备份记录与大小，可手动触发备份、下载、删除；非 admin 角色看不到该菜单 |
| 11 | 误删防护完整 | 删除备份时未成功备份受保护；数据库恢复需手工输入确认文本方可执行 |
| 12 | 图片回滚可用 | 在恢复页选择被删图片 key，能从 COS 版本历史选版本并成功回滚，页面图片恢复显示 |

## 七、建议实现要点（供开发参考，非强制）

### 7.1 图片迁移至 COS

上传替换伪代码示意（实际按项目使用的 SDK 实现）：

```ts
const key = `materials/${randomUUID()}.webp`;
await cos.putObject({ Bucket, Key: key, Body: buffer, ContentType: 'image/webp' });
// 数据库仅存相对 key
await prisma.materialImage.create({ data: { materialId, url: key, thumbnailUrl: thumbKey } });
```

前端访问时拼接配置的 CDN/COS 域名，例如 `${IMAGE_BASE_URL}/${key}`。

### 7.2 数据库加密备份

```bash
docker exec <postgres_container> pg_dump -U <user> <dbname> \
  | gzip \
  | gpg --symmetric --cipher-algo AES256 --batch \
        --passphrase-file /root/.backup-pass \
  > /backup/fabric-erp-db-$(date +%Y%m%d).sql.gz.gpg
```

口令文件权限设为 600，且与备份文件分开存放。

### 7.3 图片引用清单导出

```sql
SELECT url AS path FROM material_image
UNION ALL SELECT thumbnail_url FROM material_image WHERE thumbnail_url IS NOT NULL
UNION ALL SELECT image FROM material_color WHERE image IS NOT NULL
UNION ALL SELECT logo_url FROM company_info WHERE logo_url IS NOT NULL;
```

### 7.4 恢复流程

```bash
gpg --decrypt --batch --passphrase-file /root/.backup-pass db-20260915.sql.gz.gpg \
  | gunzip \
  | docker exec -i <postgres_container> psql -U <user> <dbname>
```

### 7.5 恢复校验 SQL

```sql
SELECT 'user_account' AS t, count(*) FROM user_account
UNION ALL SELECT 'material_fabric', count(*) FROM material_fabric
UNION ALL SELECT 'sample_choose', count(*) FROM sample_choose;
```

### 7.7 前端接入要点

- 菜单：在 `src/config/menu.ts` 的「系统管理」组内新增 `/system/backups`，`roles: ['admin']`，图标建议 `ArchiveRestore`（与现有 `Settings`、`ScrollText` 风格一致）。
- 列表：复用现有 `DataTable` 组件（`src/components/DataTable.tsx`），支持分页参数。
- 筛选：复用 `FilterBar`，页面结构参考 `src/pages/AiConfigSettings.tsx`（同属系统管理下的设置类页面）。
- 路由：在 `src/App.tsx` 中注册，并用 `RequireRole` 包裹，保证非 admin 无法直接访问 URL。
- 恢复入口：作为备份列表行内操作 + 顶部按钮，不新增菜单项。

### 7.8 注意事项

- 恢复前须确认 `_prisma_migrations` 表已一并恢复，否则 `prisma migrate deploy` 会重复执行迁移导致报错。
- 恢复顺序：恢复数据库 → `npx prisma migrate deploy` 确认迁移状态 → 校验图片对象可访问 → 启动应用。
- 若应用已升级过 schema，恢复旧备份后需按迁移记录判断是否补跑迁移。
- `JWT_ACCESS_SECRET` / `JWT_REFRESH_SECRET` 变更会使所有已登录用户 refresh token 失效，恢复环境时须沿用原值。
- COS 上的图片访问依赖网络，建议前置 CDN 以保障性能并降低成本。
- 若后续启用 COS 跨区域复制，可进一步提升容灾等级，本需求不强制。

## 八、交付物

1. 图片迁移脚本（本地 Volume → COS）与校验报告
2. 数据库加密备份脚本 + 定时任务配置（cron 或等效方案）
3. 恢复脚本与操作文档
4. COS 版本控制与生命周期配置说明
5. `seed.ts` 安全隐患修复（移除硬编码密码、停止重置已有账号密码）
6. 备份管理页与恢复页前端实现（含菜单、路由、权限控制）
7. 备份相关的后端接口（列表、触发、下载、删除、恢复、图片版本回滚）
8. 一次恢复演练记录

## 九、第一阶段实施与验证记录

### 9.1 已完成范围（图片上 COS 之前的部分）

| 需求 | 实现 | 验证结果 |
| --- | --- | --- |
| FR-3 数据库备份 | [backup.service.ts](../api/src/services/backup.service.ts) `runDatabaseBackup`，pg_dump → gzip → AES-256-GCM | 通过，产出 11 KB 加密文件 |
| FR-3.4 备份加密 | Node `crypto` AES-256-GCM，scrypt 派生密钥，文件头带 MAGIC 标识 | 通过，文件头为 `S2ABK001`，无明文内容 |
| FR-4 图片引用清单 | `runManifestBackup`，汇总 4 类图片来源导出 CSV | 通过 |
| FR-7 保留策略 | `cleanupExpiredBackups`，保护手动备份与最近一次成功备份 | 通过 |
| FR-8 恢复能力 | [restore-backup.ts](../api/scripts/restore-backup.ts) 配套恢复脚本 | 通过，见 9.3 演练 |
| FR-10 备份管理页 | [BackupManagement.tsx](../src/pages/BackupManagement.tsx) | 通过，见 9.4 |
| FR-11 恢复引导页 | 同页内对话框，二次确认 + 数据比对 + 服务器指引 | 通过 |
| FR-5.4 seed 隐患 | [seed.ts](../api/prisma/seed.ts) 改为仅创建时写密码 | 通过 |

### 9.2 实施中的关键决策

1. **压缩与加密改用 Node 内置能力**。原方案依赖 `gzip`、`gpg` 外部命令，实测 Windows 环境两者均不存在，备份直接失败。改为 `zlib` + `crypto` 后无外部依赖，容器与本地行为一致。
2. **加密格式自定义**：`MAGIC(8) + salt(16) + iv(12) + 密文 + authTag(16)`。因 GCM 认证标签位于流末尾，解密时先定位尾部 16 字节再流式解密，避免全量读入内存。
3. **不提供一键恢复接口**。数据库恢复会覆盖生产数据，后端只返回「恢复指引」，恢复由运维在服务器执行脚本，从设计上消除误点风险。
4. **恢复脚本内置安全校验**：拒绝恢复到 `DATABASE_URL` 指向的库，未加 `--yes` 时需人工确认。
5. **服务启动时清理中断任务**：重启后把残留 `RUNNING` 记录标记为失败，避免阻塞后续备份。

### 9.3 恢复演练结果

对加密备份执行完整恢复流程，导入独立测试库后逐表比对行数：

| 表 | 备份时 | 恢复后 | 结果 |
| --- | --- | --- | --- |
| company_info | 1 | 1 | 一致 |
| customer | 1 | 1 | 一致 |
| data_dictionary | 49 | 49 | 一致 |
| material_category | 1 | 1 | 一致 |
| material_color | 0 | 0 | 一致 |
| material_fabric | 1 | 1 | 一致 |
| material_image | 0 | 0 | 一致 |
| provider | 1 | 1 | 一致 |
| role | 2 | 2 | 一致 |
| sample_choose | 0 | 0 | 一致 |
| sample_choose_item | 0 | 0 | 一致 |
| user_account | 2 | 2 | 一致 |

`_prisma_migrations` 表 14 条迁移记录随备份一同恢复，符合 FR-3.1 要求。结论：恢复演练通过。

### 9.4 接口与页面验证

| 验证项 | 结果 |
| --- | --- |
| 管理员访问备份列表 | 正常，返回记录、分页与磁盘占用 |
| 员工访问备份接口 | 403 拒绝 |
| 未登录访问备份接口 | 401 拒绝 |
| 重复触发备份 | 409 拒绝，避免并发 |
| 删除最近一次成功备份 | 400 拒绝 |
| 删除手动触发的备份 | 400 拒绝 |
| 下载加密备份 | 正常，11 KB |
| 数据比对接口 | 返回 12 张表行数差异 |
| 备份页渲染 | 菜单、概览条、筛选、表格、加密与状态标识均正常 |
| 恢复对话框 | 风险提示、比对表、确认按钮默认禁用 |
| 二次确认 | 输入「确认恢复」后按钮才可点击 |
| 恢复指引 | 正确输出服务器执行步骤 |

### 9.5 尚未实施（待 COS 账号到位）

- FR-1 图片迁移至 COS
- FR-2 COS 版本控制与生命周期配置
- FR-8.2 单张图片版本回滚
- 前端图片访问地址切换为 COS/CDN 域名
- 现有上传、删除逻辑由本地文件改为对象存储操作
- 存量图片一次性迁移与校验
